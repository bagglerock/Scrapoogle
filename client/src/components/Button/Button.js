import React from "react";

import button from "./Button.css";

const Button = () => {
    return (
        <div>
            <button className={button.button}>Update</button>
        </div>
    )
}

export default Button;